import { colors } from '@/constants/theme';

/** Legacy export kept for template leftovers; prefer constants/theme. */
const Colors = {
  light: {
    text: colors.text,
    background: colors.bg,
    tint: colors.accent,
    tabIconDefault: colors.tabInactive,
    tabIconSelected: colors.accent,
  },
  dark: {
    text: colors.text,
    background: colors.bg,
    tint: colors.accent,
    tabIconDefault: colors.tabInactive,
    tabIconSelected: colors.accent,
  },
};

export default Colors;
